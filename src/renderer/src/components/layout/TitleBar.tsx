export function TitleBar(): JSX.Element {
  return (
    <div className="drag-region h-12 flex items-center justify-end px-4 bg-bg-surface/50 border-b border-border-subtle transition-colors duration-200">
      {/* macOS traffic lights are in the sidebar; this is just a drag region */}
    </div>
  )
}
